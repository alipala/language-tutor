[phases.setup]
pythonVersion = "3.11"
aptPkgs = ["python3-dev", "python3-venv", "build-essential"]

[phases.install]
cmd = "python3 -m pip install --no-cache-dir -r requirements.txt"

[start]
cmd = "/opt/venv/bin/python challenge_pool_replenisher.py"
