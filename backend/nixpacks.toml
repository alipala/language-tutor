[phases.setup]
pythonVersion = "3.11"
aptPkgs = ["python3-pip", "python3-dev", "build-essential"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.cron.txt"]

[start]
cmd = "python challenge_pool_replenisher.py"
