# Core FastAPI and server dependencies
fastapi==0.115.11
uvicorn==0.34.0
python-dotenv~=1.1.1
httpx==0.28.1
pydantic~=2.11.9
python-multipart==0.0.9

# OpenAI API - Updated for GPT-5-mini support
# Pinned floor to 2.30.0: required for gpt-4o-transcribe-diarize + chunking_strategy
# (file transcription) and gpt-realtime-whisper (realtime transcription). Earlier
# versions may not ship these model schemas, and >= alone lets Railway pull an
# unverified build at deploy time.
openai>=2.30.0,<3

# MongoDB and authentication packages
# Pin specific compatible versions to avoid import errors
pymongo==4.6.1
motor==3.3.2
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
email-validator==2.1.1
PyJWT>=2.10.1

# Google Auth packages
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.108.0

# PDF generation packages
reportlab==4.0.9

# Modern PDF generation with WeasyPrint - install with specific versions to avoid conflicts
weasyprint==61.2
cffi==1.16.0
cairocffi==1.6.1
cairosvg==2.7.1
cssselect2==0.7.0
html5lib==1.1
pyphen==0.14.0
tinycss2==1.2.1

# Template and visualization dependencies
jinja2==3.1.4
plotly==5.17.0
matplotlib==3.8.4
seaborn==0.13.2
kaleido==0.2.1

# Vector chatbot dependencies - use compatible versions
numpy==1.24.4
scikit-learn==1.4.2
pinecone-client==5.0.1

# Stripe payment processing
stripe==11.1.1

# Date utilities for subscription management
python-dateutil==2.8.2

# Real-time notification system
exponent-server-sdk==2.2.0
websockets==12.0

# Background job scheduler
schedule==1.2.0
pytz==2024.1

# CrewAI Multi-Agent System (Python 3.10-3.13) - Using pre-release with relaxed openai constraints
# Note: crewai-tools is now bundled in crewai monorepo (no separate package needed)
crewai==1.10.0a1

# Additional dependencies for stability
pillow==10.3.0
lxml==5.2.1

# Audio processing for Speaking DNA feature
librosa==0.10.1
# soundfile 0.12.1 links against a libsndfile build that can lack WAV support
# (observed locally: sf.available_formats() had no 'WAV', so every acoustic
# analysis failed with "Format not recognised" and DNA fell back to
# acoustic_metrics_captured=False). 0.14.x bundles libsndfile 1.2.2 with WAV in
# the manylinux_2_28_x86_64 wheel, so no apt package is needed on Railway.
# Upper bound is deliberate: this pin exists to fix a native-library problem, so
# a future minor must be verified against sf.available_formats(), not picked up
# silently by a redeploy.
soundfile>=0.14.0,<0.15
praat-parselmouth==0.4.3
# pkg_resources was removed in setuptools 81+; librosa's lazy audio path still
# imports it, which surfaced as "No module named 'pkg_resources'" at runtime
# (import-time was fine, so it only failed mid-assessment). Railway's build
# already pins setuptools==69.5.1 before this file is installed (see
# nixpacks.toml / Dockerfile), so this is a belt-and-braces guard for anyone
# building without that step.
setuptools<81

# Apple StoreKit 2 signed-transaction verification (Apple's official library).
# react-native-iap v14 returns a JWS, and under StoreKit 2 the on-device receipt
# file often does not exist at all, so the legacy /verifyReceipt path cannot be
# relied on. Verification is offline against the root certs in certs/apple.
app-store-server-library==3.1.2

# Azure Speech SDK for pronunciation assessment
azure-cognitiveservices-speech==1.41.1

# Redis caching for performance optimization
redis==5.0.1
async-timeout>=4.0.2
openpyxl>=3.1.2
