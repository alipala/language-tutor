# Phase 2: CrewAI Multi-Agent Challenge Generation Requirements
# ================================================================
# Updated to support CrewAI weekly cron job

# CrewAI multi-agent system (includes openai, python-dotenv, pydantic, httpx, etc.)
# NOTE: Requires Python >=3.10, <3.14
crewai==1.7.1
crewai-tools==1.7.1

# Database drivers (not provided by CrewAI)
pymongo==4.6.1
motor==3.3.2

# News generation dependencies (news_generation/ module)
pytz>=2024.1       # Timezone handling for CET scheduler
requests>=2.31.0   # NewsAPI HTTP calls in news_tools.py

# Note: CrewAI automatically includes its dependencies:
# - openai (compatible version)
# - python-dotenv
# - pydantic
# - httpx
# - and other required packages
