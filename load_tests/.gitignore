# Load test result archives — never commit
results/
*.json.gz

# Local overrides
*.local.js
# data/users.json intentionally NOT gitignored — k6 bundles it for cloud runs
# It contains short-lived JWTs for dedicated load-test accounts only
